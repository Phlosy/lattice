<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Bun (JavaScriptCore) JIT; required when the sidecar is signed with the
       hardened runtime (--options runtime) for Developer ID distribution. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Pi loads optional native add-ons / dylibs at runtime. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
